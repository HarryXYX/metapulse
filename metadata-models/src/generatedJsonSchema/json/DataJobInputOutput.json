{
  "definitions" : {
    "DataJobInputOutput" : {
      "description" : "Information about the inputs and outputs of a Data processing job",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "inputDatasets" : {
          "description" : "Input datasets consumed by the data job during processing\nDeprecated! Use inputDatasetEdges instead.",
          "type" : "array",
          "items" : {
            "type" : "string"
          }
        },
        "inputDatasetEdges" : {
          "default" : null,
          "description" : "Input datasets consumed by the data job during processing",
          "type" : "array",
          "items" : {
            "$ref" : "#/definitions/Edge"
          }
        },
        "outputDatasets" : {
          "description" : "Output datasets produced by the data job during processing\nDeprecated! Use outputDatasetEdges instead.",
          "type" : "array",
          "items" : {
            "type" : "string"
          }
        },
        "outputDatasetEdges" : {
          "default" : null,
          "description" : "Output datasets produced by the data job during processing",
          "type" : "array",
          "items" : {
            "$ref" : "#/definitions/Edge"
          }
        },
        "inputDatajobs" : {
          "default" : null,
          "description" : "Input datajobs that this data job depends on\nDeprecated! Use inputDatajobEdges instead.",
          "type" : "array",
          "items" : {
            "type" : "string"
          }
        },
        "inputDatajobEdges" : {
          "default" : null,
          "description" : "Input datajobs that this data job depends on",
          "type" : "array",
          "items" : {
            "$ref" : "#/definitions/Edge"
          }
        },
        "inputDatasetFields" : {
          "default" : null,
          "description" : "Fields of the input datasets used by this job",
          "type" : "array",
          "items" : {
            "type" : "string"
          }
        },
        "outputDatasetFields" : {
          "default" : null,
          "description" : "Fields of the output datasets this job writes to",
          "type" : "array",
          "items" : {
            "type" : "string"
          }
        },
        "fineGrainedLineages" : {
          "default" : null,
          "description" : "Fine-grained column-level lineages\nNot currently supported in the UI\nUse UpstreamLineage aspect for datasets to express Column Level Lineage for the UI",
          "type" : "array",
          "items" : {
            "$ref" : "#/definitions/FineGrainedLineage"
          }
        }
      },
      "required" : [ "inputDatasets", "outputDatasets" ]
    },
    "Edge" : {
      "description" : "A common structure to represent all edges to entities when used inside aspects as collections\nThis ensures that all edges have common structure around audit-stamps and will support PATCH, time-travel automatically.",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "sourceUrn" : {
          "default" : null,
          "description" : "Urn of the source of this relationship edge.\nIf not specified, assumed to be the entity that this aspect belongs to.",
          "type" : "string"
        },
        "destinationUrn" : {
          "description" : "Urn of the destination of this relationship edge.",
          "type" : "string"
        },
        "created" : {
          "default" : null,
          "description" : "Audit stamp containing who created this relationship edge and when",
          "$ref" : "#/definitions/AuditStamp"
        },
        "lastModified" : {
          "default" : null,
          "description" : "Audit stamp containing who last modified this relationship edge and when",
          "$ref" : "#/definitions/AuditStamp"
        },
        "properties" : {
          "default" : null,
          "description" : "A generic properties bag that allows us to store specific information on this graph edge.",
          "type" : "object",
          "additionalProperties" : {
            "type" : "string"
          }
        }
      },
      "required" : [ "destinationUrn" ]
    },
    "AuditStamp" : {
      "description" : "Data captured on a resource/association/sub-resource level giving insight into when that resource/association/sub-resource moved into a particular lifecycle stage, and who acted to move it into that specific lifecycle stage.",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "time" : {
          "description" : "When did the resource/association/sub-resource move into the specific lifecycle stage represented by this AuditEvent.",
          "type" : "integer",
          "format" : "int64",
          "minimum" : -9223372036854775808,
          "maximum" : 9223372036854775807
        },
        "actor" : {
          "description" : "The entity (e.g. a member URN) which will be credited for moving the resource/association/sub-resource into the specific lifecycle stage. It is also the one used to authorize the change.",
          "type" : "string"
        },
        "impersonator" : {
          "default" : null,
          "description" : "The entity (e.g. a service URN) which performs the change on behalf of the Actor and must be authorized to act as the Actor.",
          "type" : "string"
        },
        "message" : {
          "default" : null,
          "description" : "Additional context around how DataHub was informed of the particular change. For example: was the change created by an automated process, or manually.",
          "type" : "string"
        }
      },
      "required" : [ "time", "actor" ]
    },
    "FineGrainedLineage" : {
      "description" : "A fine-grained lineage from upstream fields/datasets to downstream field(s)",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "upstreamType" : {
          "description" : "The type of upstream entity",
          "$ref" : "#/definitions/FineGrainedLineageUpstreamType"
        },
        "upstreams" : {
          "default" : null,
          "description" : "Upstream entities in the lineage",
          "type" : "array",
          "items" : {
            "type" : "string"
          }
        },
        "downstreamType" : {
          "description" : "The type of downstream field(s)",
          "$ref" : "#/definitions/FineGrainedLineageDownstreamType"
        },
        "downstreams" : {
          "default" : null,
          "description" : "Downstream fields in the lineage",
          "type" : "array",
          "items" : {
            "type" : "string"
          }
        },
        "transformOperation" : {
          "default" : null,
          "description" : "The transform operation applied to the upstream entities to produce the downstream field(s)",
          "type" : "string"
        },
        "confidenceScore" : {
          "default" : 1,
          "description" : "The confidence in this lineage between 0 (low confidence) and 1 (high confidence)",
          "type" : "number"
        },
        "query" : {
          "default" : null,
          "description" : "The query that was used to generate this lineage. \nPresent only if the lineage was generated from a detected query.",
          "type" : "string"
        }
      },
      "required" : [ "upstreamType", "downstreamType", "confidenceScore" ]
    },
    "FineGrainedLineageUpstreamType" : {
      "description" : "The type of upstream entity in a fine-grained lineage",
      "enum" : [ "FIELD_SET", "DATASET", "NONE" ]
    },
    "FineGrainedLineageDownstreamType" : {
      "description" : "The type of downstream field(s) in a fine-grained lineage",
      "enum" : [ "FIELD", "FIELD_SET" ]
    }
  },
  "$ref" : "#/definitions/DataJobInputOutput"
}