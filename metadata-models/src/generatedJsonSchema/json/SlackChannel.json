{
  "definitions" : {
    "SlackChannel" : {
      "description" : "Information about a Slack channel.\nNamespace for the urn of this entity should be 'slack-channel'. See GenericEntityKey.pdl for details.",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "id" : {
          "description" : "The unique ID of the Slack entity. I.e., memberID, or channelID.",
          "type" : "string"
        },
        "displayName" : {
          "description" : "The display name of the Slack entity. I.e., if this is a member, it would be the member's name.",
          "type" : "string"
        },
        "topic" : {
          "default" : null,
          "description" : "The topic of this Slack channel, if set.",
          "type" : "string"
        }
      },
      "required" : [ "id", "displayName" ]
    }
  },
  "$ref" : "#/definitions/SlackChannel"
}