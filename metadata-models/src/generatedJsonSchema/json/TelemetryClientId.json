{
  "definitions" : {
    "TelemetryClientId" : {
      "description" : "A simple wrapper around a String to persist the client ID for telemetry in DataHub's backend DB",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "clientId" : {
          "description" : "A string representing the telemetry client ID",
          "type" : "string"
        }
      },
      "required" : [ "clientId" ]
    }
  },
  "$ref" : "#/definitions/TelemetryClientId"
}