{
  "definitions" : {
    "QueryProperties" : {
      "description" : "Information about a Query against one or more data assets (e.g. Tables or Views).",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "customProperties" : {
          "default" : { },
          "description" : "Custom property bag.",
          "type" : "object",
          "additionalProperties" : {
            "type" : "string"
          }
        },
        "statement" : {
          "description" : "The Query Statement.",
          "$ref" : "#/definitions/QueryStatement"
        },
        "source" : {
          "description" : "The source of the Query",
          "$ref" : "#/definitions/QuerySource"
        },
        "name" : {
          "default" : null,
          "description" : "Optional display name to identify the query.",
          "type" : "string"
        },
        "description" : {
          "default" : null,
          "description" : "The Query description.",
          "type" : "string"
        },
        "created" : {
          "description" : "Audit stamp capturing the time and actor who created the Query.",
          "$ref" : "#/definitions/AuditStamp"
        },
        "lastModified" : {
          "description" : "Audit stamp capturing the time and actor who last modified the Query.",
          "$ref" : "#/definitions/AuditStamp"
        },
        "origin" : {
          "default" : null,
          "description" : "The origin of the Query.\nThis is the source of the Query (e.g. a View, Stored Procedure, dbt Model, etc.) that the Query was created from.",
          "type" : "string"
        }
      },
      "required" : [ "customProperties", "statement", "source", "created", "lastModified" ]
    },
    "QueryStatement" : {
      "description" : "A query statement against one or more data assets.",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "value" : {
          "description" : "The query text",
          "type" : "string"
        },
        "language" : {
          "default" : "SQL",
          "description" : "The language of the Query, e.g. SQL.",
          "$ref" : "#/definitions/QueryLanguage"
        }
      },
      "required" : [ "value", "language" ]
    },
    "QueryLanguage" : {
      "enum" : [ "SQL", "UNKNOWN" ]
    },
    "QuerySource" : {
      "enum" : [ "MANUAL", "SYSTEM" ]
    },
    "AuditStamp" : {
      "description" : "Data captured on a resource/association/sub-resource level giving insight into when that resource/association/sub-resource moved into a particular lifecycle stage, and who acted to move it into that specific lifecycle stage.",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "time" : {
          "description" : "When did the resource/association/sub-resource move into the specific lifecycle stage represented by this AuditEvent.",
          "type" : "integer",
          "format" : "int64",
          "minimum" : -9223372036854775808,
          "maximum" : 9223372036854775807
        },
        "actor" : {
          "description" : "The entity (e.g. a member URN) which will be credited for moving the resource/association/sub-resource into the specific lifecycle stage. It is also the one used to authorize the change.",
          "type" : "string"
        },
        "impersonator" : {
          "default" : null,
          "description" : "The entity (e.g. a service URN) which performs the change on behalf of the Actor and must be authorized to act as the Actor.",
          "type" : "string"
        },
        "message" : {
          "default" : null,
          "description" : "Additional context around how DataHub was informed of the particular change. For example: was the change created by an automated process, or manually.",
          "type" : "string"
        }
      },
      "required" : [ "time", "actor" ]
    }
  },
  "$ref" : "#/definitions/QueryProperties"
}