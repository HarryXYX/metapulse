{
  "definitions" : {
    "DataHubIngestionSourceSchedule" : {
      "description" : "The schedule associated with an ingestion source.",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "interval" : {
          "description" : "A cron-formatted execution interval, as a cron string, e.g. * * * * *",
          "type" : "string"
        },
        "timezone" : {
          "description" : "Timezone in which the cron interval applies, e.g. America/Los Angeles",
          "type" : "string"
        }
      },
      "required" : [ "interval", "timezone" ]
    }
  },
  "$ref" : "#/definitions/DataHubIngestionSourceSchedule"
}