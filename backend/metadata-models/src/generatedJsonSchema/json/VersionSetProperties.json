{
  "definitions" : {
    "VersionSetProperties" : {
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "customProperties" : {
          "default" : { },
          "description" : "Custom property bag.",
          "type" : "object",
          "additionalProperties" : {
            "type" : "string"
          }
        },
        "latest" : {
          "description" : "The latest versioned entity linked to in this version set",
          "type" : "string"
        },
        "versioningScheme" : {
          "description" : "What versioning scheme is being utilized for the versioned entities sort criterion. Static once set",
          "$ref" : "#/definitions/VersioningScheme"
        }
      },
      "required" : [ "customProperties", "latest", "versioningScheme" ]
    },
    "VersioningScheme" : {
      "enum" : [ "LEXICOGRAPHIC_STRING", "ALPHANUMERIC_GENERATED_BY_DATAHUB" ]
    }
  },
  "$ref" : "#/definitions/VersionSetProperties"
}