{
  "definitions" : {
    "DataProcessInstanceRelationships" : {
      "description" : "Information about Data process relationships",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "parentTemplate" : {
          "default" : null,
          "description" : "The parent entity whose run instance it is",
          "type" : "string"
        },
        "parentInstance" : {
          "default" : null,
          "description" : "The parent DataProcessInstance where it belongs to.\nIf it is a Airflow Task then it should belong to an Airflow Dag run as well\nwhich will be another DataProcessInstance",
          "type" : "string"
        },
        "upstreamInstances" : {
          "description" : "Input DataProcessInstance which triggered this dataprocess instance",
          "type" : "array",
          "items" : {
            "type" : "string"
          }
        }
      },
      "required" : [ "upstreamInstances" ]
    }
  },
  "$ref" : "#/definitions/DataProcessInstanceRelationships"
}