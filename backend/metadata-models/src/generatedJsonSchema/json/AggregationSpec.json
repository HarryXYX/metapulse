{
  "definitions" : {
    "AggregationSpec" : {
      "description" : "Provides the aggregation specification on a member/field.",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "fieldPath" : {
          "description" : "Name of the member/field.",
          "type" : "string"
        },
        "aggregationType" : {
          "description" : "Aggregation type for the metric.",
          "$ref" : "#/definitions/AggregationType"
        }
      },
      "required" : [ "fieldPath", "aggregationType" ]
    },
    "AggregationType" : {
      "enum" : [ "LATEST", "SUM", "CARDINALITY" ]
    }
  },
  "$ref" : "#/definitions/AggregationSpec"
}