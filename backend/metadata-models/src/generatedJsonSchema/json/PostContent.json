{
  "definitions" : {
    "PostContent" : {
      "description" : "Content stored inside a Post.",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "title" : {
          "description" : "Title of the post.",
          "type" : "string"
        },
        "type" : {
          "description" : "Type of content held in the post.",
          "$ref" : "#/definitions/PostContentType"
        },
        "description" : {
          "default" : null,
          "description" : "Optional description of the post.",
          "type" : "string"
        },
        "link" : {
          "default" : null,
          "description" : "Optional link that the post is associated with.",
          "type" : "string"
        },
        "media" : {
          "default" : null,
          "description" : "Optional media that the post is storing",
          "$ref" : "#/definitions/Media"
        }
      },
      "required" : [ "title", "type" ]
    },
    "PostContentType" : {
      "description" : "Enum defining the type of content held in a Post.",
      "enum" : [ "TEXT", "LINK" ]
    },
    "Media" : {
      "description" : "Carries information about which roles a user is assigned to.",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "type" : {
          "description" : "Type of content the Media is storing, e.g. image, video, etc.",
          "$ref" : "#/definitions/MediaType"
        },
        "location" : {
          "description" : "Where the media content is stored.",
          "type" : "string"
        }
      },
      "required" : [ "type", "location" ]
    },
    "MediaType" : {
      "description" : "Enum defining the type of content a Media object holds.",
      "enum" : [ "IMAGE" ]
    }
  },
  "$ref" : "#/definitions/PostContent"
}