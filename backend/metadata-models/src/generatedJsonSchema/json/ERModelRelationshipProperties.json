{
  "definitions" : {
    "ERModelRelationshipProperties" : {
      "description" : "Properties associated with a ERModelRelationship",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "customProperties" : {
          "default" : { },
          "description" : "Custom property bag.",
          "type" : "object",
          "additionalProperties" : {
            "type" : "string"
          }
        },
        "name" : {
          "description" : "Name of the ERModelRelation",
          "type" : "string"
        },
        "source" : {
          "description" : "First dataset in the erModelRelationship (no directionality)",
          "type" : "string"
        },
        "destination" : {
          "description" : "Second dataset in the erModelRelationship (no directionality)",
          "type" : "string"
        },
        "relationshipFieldMappings" : {
          "description" : "ERModelRelationFieldMapping (in future we can make it an array)",
          "type" : "array",
          "items" : {
            "$ref" : "#/definitions/RelationshipFieldMapping"
          }
        },
        "created" : {
          "default" : null,
          "description" : "A timestamp documenting when the asset was created in the source Data Platform (not on DataHub)",
          "$ref" : "#/definitions/AuditStamp"
        },
        "lastModified" : {
          "default" : null,
          "description" : "A timestamp documenting when the asset was last modified in the source Data Platform (not on DataHub)",
          "$ref" : "#/definitions/AuditStamp"
        },
        "cardinality" : {
          "default" : "N_N",
          "description" : "Cardinality of the relationship",
          "$ref" : "#/definitions/ERModelRelationshipCardinality"
        }
      },
      "required" : [ "customProperties", "name", "source", "destination", "relationshipFieldMappings", "cardinality" ]
    },
    "RelationshipFieldMapping" : {
      "description" : "Individual Field Mapping of a relationship- one of several",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "sourceField" : {
          "description" : "All fields from dataset A that are required for the join, maps to bFields 1:1",
          "type" : "string"
        },
        "destinationField" : {
          "description" : "All fields from dataset B that are required for the join, maps to aFields 1:1",
          "type" : "string"
        }
      },
      "required" : [ "sourceField", "destinationField" ]
    },
    "AuditStamp" : {
      "description" : "Data captured on a resource/association/sub-resource level giving insight into when that resource/association/sub-resource moved into a particular lifecycle stage, and who acted to move it into that specific lifecycle stage.",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "time" : {
          "description" : "When did the resource/association/sub-resource move into the specific lifecycle stage represented by this AuditEvent.",
          "type" : "integer",
          "format" : "int64",
          "minimum" : -9223372036854775808,
          "maximum" : 9223372036854775807
        },
        "actor" : {
          "description" : "The entity (e.g. a member URN) which will be credited for moving the resource/association/sub-resource into the specific lifecycle stage. It is also the one used to authorize the change.",
          "type" : "string"
        },
        "impersonator" : {
          "default" : null,
          "description" : "The entity (e.g. a service URN) which performs the change on behalf of the Actor and must be authorized to act as the Actor.",
          "type" : "string"
        },
        "message" : {
          "default" : null,
          "description" : "Additional context around how DataHub was informed of the particular change. For example: was the change created by an automated process, or manually.",
          "type" : "string"
        }
      },
      "required" : [ "time", "actor" ]
    },
    "ERModelRelationshipCardinality" : {
      "enum" : [ "ONE_ONE", "ONE_N", "N_ONE", "N_N" ]
    }
  },
  "$ref" : "#/definitions/ERModelRelationshipProperties"
}