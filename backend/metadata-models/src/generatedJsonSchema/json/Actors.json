{
  "definitions" : {
    "Actors" : {
      "description" : "Provisioned users and groups of a role",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "users" : {
          "default" : null,
          "description" : "List of provisioned users of a role",
          "type" : "array",
          "items" : {
            "$ref" : "#/definitions/RoleUser"
          }
        },
        "groups" : {
          "default" : null,
          "description" : "List of provisioned groups of a role",
          "type" : "array",
          "items" : {
            "$ref" : "#/definitions/RoleGroup"
          }
        }
      }
    },
    "RoleUser" : {
      "description" : "Provisioned users of a role",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "user" : {
          "description" : "Link provisioned corp user for a role",
          "type" : "string"
        }
      },
      "required" : [ "user" ]
    },
    "RoleGroup" : {
      "description" : "Provisioned groups of a role",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "group" : {
          "description" : "Link provisioned corp group for a role",
          "type" : "string"
        }
      },
      "required" : [ "group" ]
    }
  },
  "$ref" : "#/definitions/Actors"
}