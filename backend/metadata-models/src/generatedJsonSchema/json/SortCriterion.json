{
  "definitions" : {
    "SortCriterion" : {
      "description" : "Sort order along with the field to sort it on, to be applied to the results.",
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "field" : {
          "description" : "The name of the field that sorting has to be applied to",
          "type" : "string"
        },
        "order" : {
          "description" : "The order to sort the results i.e. ASCENDING or DESCENDING",
          "$ref" : "#/definitions/SortOrder"
        }
      },
      "required" : [ "field", "order" ]
    },
    "SortOrder" : {
      "description" : "The order used to sort the results",
      "enum" : [ "ASCENDING", "DESCENDING" ]
    }
  },
  "$ref" : "#/definitions/SortCriterion"
}