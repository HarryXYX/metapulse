# 开发环境专用配置
# 使用方式：--spring.profiles.active=dev
# 此配置会覆盖 application.yml 中的占位符，无需设置环境变量

server:
  port: 8080
  # 增加 Jetty 异步请求超时（从默认 30 秒增加到 120 秒）
  jetty:
    threads:
      max: 200  # 最大线程数
      min: 8    # 最小线程数
    connection:
      idle-timeout: 120000  # 连接空闲超时：120 秒

# Spring MVC 异步请求配置
spring.mvc.async.request-timeout: 120000  # 120 秒

# ===== Consumer 启用开关（开发环境全部启用） =====
# 必须是顶级属性才能被Spring正确读取
MAE_CONSUMER_ENABLED: true
MCE_CONSUMER_ENABLED: true
MCP_CONSUMER_ENABLED: true
PE_CONSUMER_ENABLED: true

# ===== Entity Client配置 =====
entityClient:
  impl: java  # 本地方法调用

# ===== 数据库配置 =====
spring:
  datasource:
    url: jdbc:mysql://47.80.65.112:3306/metapulse?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true&autoReconnect=true&failOverReadOnly=false&maxReconnects=10
    username: metapulse
    password: metapulse
    driver-class-name: com.mysql.cj.jdbc.Driver

    hikari:
      maximum-pool-size: 30              # 增加连接池大小以支持并发查询
      minimum-idle: 10                   # 增加最小空闲连接
      connection-timeout: 30000          # 连接获取超时：30秒
      idle-timeout: 600000               # 空闲连接存活时间：10分钟
      max-lifetime: 1800000              # 连接最大生命周期：30分钟
      connection-test-query: SELECT 1    # 连接测试查询
      validation-timeout: 3000           # 验证超时：3秒
      leak-detection-threshold: 60000    # 连接泄漏检测：60秒
      pool-name: HikariPool-MetaPulse    # 连接池名称（便于日志追踪）

  jpa:
    show-sql: true  # 开发环境显示 SQL
    properties:
      hibernate:
        format_sql: true  # 格式化 SQL 输出

  # 开发环境禁用静态资源缓存
  web:
    resources:
      cache:
        period: 0
        cachecontrol:
          no-cache: true
          no-store: true
      chain:
        enabled: false

# ===== Kafka配置 =====
kafka:
  bootstrapServers: 47.80.65.112:9092

  schemaRegistry:
    url: http://localhost:8080/schema-registry/api
    type: INTERNAL

  # Producer配置 - 增加超时适应远程Kafka
  producer:
    acks: all
    retries: 3
    compressionType: snappy
    maxRequestSize: 1048576
    requestTimeoutMs: 120000          # 请求超时：120秒（默认30秒）
    deliveryTimeoutMs: 180000         # 投递超时：180秒（默认120秒）
    maxBlockMs: 180000                # 最大阻塞时间：180秒（默认60秒）
    # 额外的网络和超时配置
    metadataMaxAgeMs: 300000          # 元数据最大缓存时间：5分钟
    reconnectBackoffMs: 50            # 重连退避时间：50毫秒
    reconnectBackoffMaxMs: 1000       # 最大重连退避时间：1秒
    # Spring Kafka 特定配置（确保覆盖默认值）
    properties:
      request.timeout.ms: 120000      # 再次明确设置请求超时
      delivery.timeout.ms: 180000     # 再次明确设置投递超时
      max.block.ms: 180000            # 再次明确设置最大阻塞时间
      metadata.max.age.ms: 300000     # 元数据过期时间
      connections.max.idle.ms: 540000 # 连接最大空闲时间：9分钟

  consumer:
    groupId: metapulse-dev-consumers
    autoOffsetReset: earliest
    requestTimeoutMs: 120000      # 请求超时：120秒（默认30秒）

  listener:
    concurrency: 2  # 开发环境减少并发

# ===== Elasticsearch配置 =====
elasticsearch:
  host: 47.80.65.112
  port: 9200
  scheme: http
  username:
  password:
  threadCount: 2  # 开发环境减少线程

# ===== 认证配置（开发环境） =====
authentication:
  enabled: true  # 启用认证，使用 metapulse-admin/metapulse-admin 登录
  excludedPaths: /logIn,/logOut,/signUp,/schema-registry/*,/health,/health/live,/config,/config/search/export,/public-iceberg/*,/actuator/prometheus
  systemClientId: __datahub_system
  systemClientSecret: JohnSnowKnowsNothing
  tokenService:
    signingKey: +8Qx7hZjcZq21cdr9f0paEceHRw47YU5pRt4P7fWpSc=  # 256-bit key for HS256
  openRegistrationEnabled: true  # 开发环境启用开放注册（无需邀请链接）

# ===== 日志配置（开发环境更详细） =====
logging:
  level:
    root: INFO
    com.linkedin: DEBUG  # DataHub 相关代码开启 DEBUG
    com.linkedin.gms: DEBUG
    com.linkedin.metadata: DEBUG
    org.springframework: INFO
    org.springframework.web: DEBUG  # Web 请求日志
    org.apache.kafka: WARN
    org.hibernate.SQL: DEBUG  # 显示 SQL
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE  # 显示 SQL 参数
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"

# ===== 管理端点（开发环境全部开放） =====
management:
  endpoints:
    web:
      exposure:
        include: "*"  # 开发环境暴露所有端点
  endpoint:
    health:
      show-details: always  # 显示详细健康信息

# ===== DataHub GMS配置 =====
datahub:
  gms:
    host: 0.0.0.0
    port: 8080
    useSSL: false
    async:
      request-timeout-ms: 120000  # 异步请求超时：120秒（默认30秒太短）

# ===== 缓存配置 =====
cache:
  client:
    entityClient:
      useCaffeineCache: true
      statsEnabled: true

# ===== 其他配置 =====
metadataChangeProposal:
  throttle:
    updateIntervalMs: 60000

telemetry:
  enabledCli: false  # 开发环境禁用xian zh遥测
  enabledServer: false

# ===== Ebean 配置（数据库表自动创建） =====
ebean:
  username: metapulse
  password: metapulse
  url: jdbc:mysql://47.80.65.112:3306/metapulse?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true&autoReconnect=true
  driver: com.mysql.cj.jdbc.Driver
  autoCreateDdl: false  # 关闭自动建表，保留数据库数据
  # 连接池健康检查配置（避免远程MySQL连接过期问题）
  maxInactiveTimeSeconds: 180        # 连接最大空闲时间：3分钟
  heartbeatFreqSecs: 30              # 心跳检测频率：30秒
  leakTimeMinutes: 5                 # 连接泄漏检测：5分钟
  trimPoolFreqSecs: 60               # 连接池修剪频率：60秒

# ===== Entity Registry 配置 =====
# 使用 classpath 方式加载（文件在 metadata-models 模块中）
configEntityRegistry:
  path: ${ENTITY_REGISTRY_CONFIG_PATH:#{null}}  # 明确设置为 null，避免使用相对路径
  resource: classpath:entity-registry.yml

# ===== 系统更新配置 =====
# 跳过系统更新等待，开发环境快速启动
systemUpdate:
  waitForSystemUpdate: false  # 🔑 关键配置：跳过Kafka系统更新消息等待

# ===== 默认用户配置（开发环境） =====
defaultUser:
  enabled: true
  username: metapulse-admin
  password: metapulse-admin
  fullName: MetaPulse Admin
  email: admin@metapulse.com
  title: Administrator
